 1. CRIAÇÃO DO BANCO DE DADOS E TABELAS


CREATE DATABASE HOTEL;

USE HOTEL;

CREATE TABLE QUARTOS (
    ID INT PRIMARY KEY,
    NUMERO VARCHAR(50),
    CAPACIDADE INT,
    VISTA_MAR BOOLEAN,
    DIARIA DECIMAL(10,2)
);

CREATE TABLE CLIENTES (
    ID INT PRIMARY KEY,
    NOME VARCHAR(100),
    EMAIL VARCHAR(100),
    TELEFONE VARCHAR(20),
    TABELA VARCHAR(100)
);

CREATE TABLE RESERVAS (
    ID INT PRIMARY KEY,
    CLIENTE_ID INT,
    QUARTO_ID INT,
    DATA_ENTRADA DATE,
    DATA_SAIDA DATE,
    FOREIGN KEY (CLIENTE_ID) REFERENCES CLIENTES(ID),
    FOREIGN KEY (QUARTO_ID) REFERENCES QUARTOS(ID)
);



 2. INSERÇÃO DOS REGISTROS


INSERT INTO QUARTOS (ID, NUMERO, CAPACIDADE, VISTA_MAR, DIARIA)
VALUES
(1, 'QUARTO 1', 1, FALSE, 150.00),
(2, 'QUARTO 2', 5, TRUE, 750.00),
(3, 'QUARTO 3', 3, TRUE, 450.00),
(4, 'QUARTO 4', 1, FALSE, 150.00),
(5, 'QUARTO 5', 2, FALSE, 300.00);

INSERT INTO CLIENTES (ID, NOME, EMAIL, TELEFONE)
VALUES
(1, 'Miguel', 'miguel@teste.com', '(46) 9 2520-3616'),
(2, 'Alice', 'alice@teste.com', '(46) 9 3582-3775'),
(3, 'Davi', 'davi@teste.com', '(46) 9 2232-1677'),
(4, 'Julia', 'julia@teste.com', '(46) 9 3712-5526'),
(5, 'Bruna', 'bruna@teste.com', '(46) 9 2515-0737');

INSERT INTO RESERVAS (ID, CLIENTE_ID, QUARTO_ID, DATA_ENTRADA, DATA_SAIDA)
VALUES
(1, 3, 1, '2023-05-01', '2023-05-02'),
(2, 5, 2, '2023-05-06', '2023-05-07'),
(3, 2, 3, '2023-04-14', '2023-04-25'),
(4, 1, 4, '2023-01-01', '2023-01-05'),
(5, 3, 5, '2023-02-20', '2023-02-24');



3. CONSULTAS

-- A) Quartos disponíveis com vista para o mar
SELECT *
FROM QUARTOS
WHERE VISTA_MAR = TRUE;

-- B) Nome e e-mail de todos os clientes
SELECT NOME, EMAIL
FROM CLIENTES;

-- C) Quartos com capacidade para mais de 2 pessoas
SELECT *
FROM QUARTOS
WHERE CAPACIDADE > 2;

-- D) Clientes que começam com a letra B
SELECT *
FROM CLIENTES
WHERE NOME LIKE 'B%';

-- E) Reservas realizadas entre 01/05/2023 e 31/05/2023
SELECT *
FROM RESERVAS
WHERE DATA_ENTRADA BETWEEN '2023-05-01' AND '2023-05-31';


 4. FUNÇÕES DE AGREGAÇÃO

-- A) Total de quartos
SELECT COUNT(*) AS TOTAL_QUARTOS
FROM QUARTOS;

-- B) Quarto com menor capacidade
SELECT *
FROM QUARTOS
WHERE CAPACIDADE = (SELECT MIN(CAPACIDADE) FROM QUARTOS);

-- C) Quarto com maior capacidade
SELECT *
FROM QUARTOS
WHERE CAPACIDADE = (SELECT MAX(CAPACIDADE) FROM QUARTOS);

-- D) Média de capacidade dos quartos
SELECT AVG(CAPACIDADE) AS MEDIA_CAPACIDADE
FROM QUARTOS;

-- E) Total de quartos com vista para o mar
SELECT COUNT(*) AS TOTAL_QUARTOS_VISTA_MAR
FROM QUARTOS
WHERE VISTA_MAR = TRUE;


-- 5. ORDENAÇÃO

-- A) Quartos por diária em ordem decrescente
SELECT *
FROM QUARTOS
ORDER BY DIARIA DESC;

-- B) Quartos por diária em ordem crescente
SELECT *
FROM QUARTOS
ORDER BY DIARIA ASC;


-- 6. CUPOM DE DESCONTO

-- A) Adicionar o campo CUPOM
ALTER TABLE RESERVAS
ADD CUPOM VARCHAR(5);

-- B) Atualizar os cupons
UPDATE RESERVAS
SET CUPOM = 'LGTSP'
WHERE ID = 1;

UPDATE RESERVAS
SET CUPOM = 'XGTSP'
WHERE ID = 2;

UPDATE RESERVAS
SET CUPOM = 'GTSQL'
WHERE ID = 4;


-- 7. JOIN ENTRE RESERVAS E CLIENTES

SELECT
    RESERVAS.*,
    CLIENTES.NOME,
    CLIENTES.EMAIL,
    CLIENTES.TELEFONE
FROM RESERVAS
INNER JOIN CLIENTES
ON RESERVAS.CLIENTE_ID = CLIENTES.ID;


8. JOIN ENTRE RESERVAS E QUARTOS

SELECT
    RESERVAS.*,
    QUARTOS.NUMERO,
    QUARTOS.CAPACIDADE,
    QUARTOS.VISTA_MAR,
    QUARTOS.DIARIA
FROM RESERVAS
INNER JOIN QUARTOS
ON RESERVAS.QUARTO_ID = QUARTOS.ID;


9. EXCLUSÃO DE RESERVAS


DELETE FROM RESERVAS
WHERE ID IN (3, 4);


10. EXCLUSÃO DAS TABELAS E DO BANCO

-- A) Dropar a tabela RESERVAS
DROP TABLE RESERVAS;

-- B) Dropar a tabela CLIENTES
DROP TABLE CLIENTES;

-- C) Dropar a tabela QUARTOS
DROP TABLE QUARTOS;

-- D) Dropar o banco de dados HOTEL
DROP DATABASE HOTEL;